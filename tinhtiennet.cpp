#include<bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cin >> n;
    vector<string> adj(n);
    for (int i = 0; i < n; i++)
    {
        string a, b;
        cin >> a >> b;
        adj[i].push_back(a)
    }
}
